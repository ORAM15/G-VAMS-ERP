# Phase 2C.2 Acceptance Criteria

- The configured line-change ceiling is injected into the implementation task.
- The implementation task explicitly requires a minimal patch and preservation of unrelated formatting.
- The implementation task requires a `git diff --numstat` budget check before success.
- The implementation task instructs the runtime to report `blocked` when the approved improvement cannot fit safely.
- The deterministic Diff Gate threshold remains unchanged.
