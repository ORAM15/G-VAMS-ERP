# Expected Outcome

The next supervised cycle should give the implementation agent the same line-change budget enforced by the Diff Gate before coding begins. This should reduce broad rewrites and make oversized work fail as an explicit blocked implementation rather than only after an expensive implementation run.
