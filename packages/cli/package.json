{
  "name": "@oram/cli",
  "version": "0.0.0",
  "private": true,
  "description": "The `oram` command — supersedes scripts/gvams-cli.js (see docs/ORAM_SPECIFICATION_v1.md and ORAM_V3_MIGRATION_PLAN.md Section 6).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "//bin": "Not wired to a build yet -- see this package's README.md Status. `bin` below documents the intended future entry point only.",
  "bin": {
    "oram": "./dist/bin.js"
  },
  "dependencies": {
    "@oram/runtime": "workspace:*",
    "@oram/engines": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
