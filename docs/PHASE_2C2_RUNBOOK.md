# Phase 2C.2 Supervised Runbook

After merge, manually dispatch the Autonomous Evolution Agent once. Review the decision artifact, implementation evidence, and Diff Gate output. A successful cycle must produce a focused repository delta under the configured ceiling and pass deterministic validation. A blocked cycle is acceptable when the approved improvement cannot safely fit the budget; an oversized implementation must continue to fail closed.
